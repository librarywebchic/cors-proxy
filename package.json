{
  "name": "cors_proxy",
  "version": "1.0.0",
  "description": "small app for proxying Linked Data requests where CORS isn't implemented",
  "main": "index.js",
  "dependencies": {
    "cors-anywhere": "^0.2.5"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0"
}
